{
  "name": "qode",
  "version": "1.0.0",
  "private": true,
  "description": "Free, on-device QR code generator with switchable links.",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js",
    "db:init": "node scripts/db-init.js"
  },
  "dependencies": {
    "@libsql/client": "^0.15.15"
  }
}
